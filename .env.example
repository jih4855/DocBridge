# DocBridge 환경 변수 설정
# 이 파일을 .env로 복사한 후 실제 값으로 수정하세요
# cp .env.example .env

# ========================================
# 필수 설정
# ========================================

# 프로젝트 루트 폴더 경로 (spec 폴더들이 있는 상위 디렉토리)
# Docker에서 /data로 마운트됨
PROJECT_ROOT=/Users/your-username/projects

# ========================================
# 프론트엔드 설정 (빌드 시점에 결정됨)
# ========================================

# API 서버 URL (브라우저에서 접근 가능한 주소)
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket 서버 URL (실시간 파일 변경 감지용)
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# ========================================
# 백엔드 설정
# ========================================

# 데이터 디렉토리 (SQLite DB 저장 위치)
DATA_DIR=./data

# 파일 감시 방식 (Docker 환경에서는 true 권장)
WATCHDOG_USE_POLLING=false

# ========================================
# 프로덕션 배포 시 (선택)
# ========================================

# 허용할 CORS 오리진 (쉼표로 구분)
# ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# 디버그 모드 (프로덕션에서는 false)
# DEBUG=false
